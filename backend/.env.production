# Server Configuration
PORT=10000
NODE_ENV=production

# MongoDB Connection
MONGO_URI=mongodb+srv://NexusHub:Jayant007@nexushub.qm06jtl.mongodb.net/?retryWrites=true&w=majority&appName=NexusHub

# JWT Authentication
JWT_SECRET=nexushub_secret_key_change_in_production
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES_IN=7

# Frontend URL (for CORS and redirects)
FRONTEND_URL=https://nexushubb.netlify.app

# Socket.io Configuration (for real-time features)
SOCKET_CORS_ORIGIN=https://nexushubb.netlify.app

# Session Secret
SESSION_SECRET=nexushub_session_secret_key_change_in_production

# OAuth Callback URLs
GOOGLE_CALLBACK_URL=https://nexushub-backend.onrender.com/api/oauth/google/callback
GITHUB_CALLBACK_URL=https://nexushub-backend.onrender.com/api/oauth/github/callback

# Logging
LOG_LEVEL=error